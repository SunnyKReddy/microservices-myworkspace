server:
  port: 9000
spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driverClassName: org.h2.Driver
    username: sa
    password: ''
  h2:
    console:
      enabled: true
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: true
  config:
    import:
      - "application_dev.yml"
      - "application_qa.yml"
      - "application_prod.yml"
  profiles:
    active: "prod"

#Defalut configurations when no profile is selected
build:
  version: "2.0"

cards:
  message: "Welcome to EazyBank CARDS related Eval APIs"
  contactDetails:
    name: "John Doe - Developer"
    email: "john-cards@eazybank.com"
  onCallSupport:
    - (555) 555-3456
    - (555) 523-3456
